import { useEffect } from "react";
import { useParams } from "react-router-dom";
import serviceStore from "./serviceStore";

const Service = () => {
  /*const { serviceName } = useParams();

  // Use the serviceName parameter to fetch data or perform other actions
  useEffect(() => {
    // Example: Fetch data based on the serviceName
    const serviceData = serviceStore.getServiceById(serviceName);
    // Do something with the serviceData
  }, [serviceName]); // Make sure to include serviceName in the dependencies array if you use it inside useEffect
*/
  return (
    <>
    </>
  );
};

export default Service;
